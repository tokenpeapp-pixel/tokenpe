import React from "react";
import RestaurantLandingPage from "@/components/RestaurantLandingPage";

export const metadata = {
  title: 'Waitlist Management for Restaurants - TokenPe',
  description: 'Manage restaurant waitlists and reservations digitally using WhatsApp. No app required for diners.',
}

export default function RestaurantsPage() {
  return <RestaurantLandingPage />;
}
